// to show market publications
// select and link to sub page on site
import 'package:flutter/material.dart';

Widget marketView(context) {
  return Center(
        child: Container(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: <Widget>[
              Text('Marketplace here')
            ]
            )
        )
  );
}
